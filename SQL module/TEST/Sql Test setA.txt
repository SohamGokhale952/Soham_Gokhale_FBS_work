Sql Test setA
NAME:SOHAM M. GOKHALE
BATCH:JAVA JUNE OFFLINE 2025

Q1.select emp_name ,salary from employee order by desc; 

Q2.select emp_name from employee order by salary desc limit 5; 

Q3 select * from employee order by salary limit (3,5);

Q4.select count(*) from employee group by department ;

Q5.select avg(salary) as "average salary" from employee  group by department having avg(sal)>50000

Q6.select*from product where name="Laptop";

Q7.select student_name from student where student_name substring(student_name,1,3);

Q8.select student_name from student where name like "a%";

Q9.select count(*) as "count" from product  group by order_month having count(order)>100;

Q10.select sum(sales_amt) as "toatl_sales",avg(sales_amt) as "average salary amount" group by category having sum(sales_amt)>100000;

Q11.a

Q12.d
 
Q13.c

Q14.b

Q15.b

Q16.select c.region, sum(o.total_amount) from customers c inner join order o on c.customer_id =o.customer_id  group by c.region having sum(o.total_amount) > 10000;

Q17.select b.auther_id,count(s.quanity) from books b inner join sales s on b.book_id =s.book_id  group by b.auther_id having sum(s.quantity)>100;